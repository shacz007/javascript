let shoppingList=["milk","bread","apples"]
console.log(shoppingList);
//printing the array

console.log(shoppingList.length);
//printiing the array length

shoppingList[1]="banana";
console.log(shoppingList);
//updating or overwriting the array

